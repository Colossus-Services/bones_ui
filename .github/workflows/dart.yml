name: Dart CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image:  google/dart:latest

    steps:
    - uses: actions/checkout@v2
    - name: APT update
      run: apt-get update
    - name: Install Firefox
      run: apt-get install firefox-esr xorg
    - name: Install dependencies
      run: pub get
    - name: Run tests
      run: pub run test -p browser
